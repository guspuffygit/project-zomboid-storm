{
  "Sandbox_Storm": "Storm | Performance",
  "Sandbox_Storm_ServerFps": "Server FPS (tick rate + lockFps + physics FPS)",
  "Sandbox_Storm_ServerFps_tooltip": "Unified server FPS. Drives the main-loop tick gate (1000/fps ms), PerformanceSettings.getLockFPS() on the server, and the IsoPhysicsObject.update() FPS scalar — all three together. 10 = vanilla 10 TPS. Higher = faster simulation but more CPU.",
  "Sandbox_Storm_AnimalLOSTickInterval": "Animal LOS tick interval",
  "Sandbox_Storm_AnimalLOSTickInterval_tooltip": "Per-animal stride for IsoAnimal.updateLOS(). 1 = vanilla every-tick. Larger = each animal scans LOS every Nth tick (cheaper). 0 disables LOS entirely.",
  "Sandbox_Storm_ZombieCullThreshold": "Zombie cull threshold (0 = disabled)",
  "Sandbox_Storm_ZombieCullThreshold_tooltip": "Storm-controlled cull threshold. 500 = vanilla cap (default). Larger = allow more live zombies before culling. 0 disables culling entirely (no zombies ever queued for deletion).",
  "Sandbox_Storm_ServerLosThreads": "ServerLOS worker threads",
  "Sandbox_Storm_ServerLosThreads_tooltip": "Number of concurrent player LOS scans per tick. 1 = vanilla single-threaded baseline. Up to 16. The helper pool always pre-allocates 15 threads regardless; this knob only controls how many receive work each tick.",
  "Sandbox_Storm_NetDataCapMs": "Net-data drain cap (ms, 0 = disabled)",
  "Sandbox_Storm_NetDataCapMs_tooltip": "Per-outer-loop-spin wall-clock cap on time spent inside GameServer.mainLoopDealWithNetData (HIGH-priority + player-update + vehicle inbound drain combined). Default 90. When a spin exceeds the cap, subsequent packets in that spin are short-circuited (RakNet retransmits any reliable ones); the next spin starts fresh. Protects world-tick scheduling and the RakNet outbound send buffer during reconnect storms. 0 = disabled (vanilla behaviour, no cap). Counter: pz_netdata_deferred_total.",
  "Sandbox_Storm_PeerSendBufferKickMb": "Auto-kick peer when HIGH send buffer exceeds (MB, 0 = disabled)",
  "Sandbox_Storm_PeerSendBufferKickMb_tooltip": "If a peer's outbound HIGH-priority RakNet send buffer stays above this many megabytes for Storm.PeerSendBufferKickHoldTicks consecutive ticks, Storm force-disconnects the connection with reason storm-send-buffer-overflow. Protects the server from OOM when a peer on a saturated or lossy uplink accumulates the server's broadcast firehose in their per-peer RakNet queue (PZ has no backpressure anywhere in the HIGH-priority send paths, so a stalled peer otherwise grows unbounded until OOM). Default 20 MB. 0 = disabled (no auto-kick). Per-peer gauge: storm_peer_send_buffer_bytes{priority=\"high\"}. Counter: storm_peer_kicked_send_buffer_total.",
  "Sandbox_Storm_PeerSendBufferKickHoldTicks": "Send-buffer kick hold ticks (consecutive ticks above threshold)",
  "Sandbox_Storm_PeerSendBufferKickHoldTicks_tooltip": "Number of consecutive server ticks a peer's HIGH send buffer must stay above Storm.PeerSendBufferKickMb before Storm force-disconnects them. At the vanilla 10 TPS, 50 ticks = 5 seconds (default). Lower values fire faster but risk false-positiving brief broadcast spikes; higher values give more grace before action. Range 1-6000 (≈10 min at 10 TPS). Has no effect when Storm.PeerSendBufferKickMb = 0 (watchdog disabled).",
  "Sandbox_Storm_ScreenshotPiecesPerPacket": "Screenshot pieces per packet (wire framing)",
  "Sandbox_Storm_ScreenshotPiecesPerPacket_tooltip": "Number of base64 pieces (24573 raw bytes each) packed into a single sendClientCommand packet when a client uploads a /screenshot back to the server. This is only wire framing (packet size), NOT the upload throttle — disconnect safety is governed by Storm.ScreenshotUploadKbPerSec. Fewer, larger packets mean fewer sendClientCommand calls. Default 4 (~131 KB/packet). Hard ceiling 28 (~918 KB/packet, vanilla UdpConnection's 1 MB outbound buffer cap with ~82 KB headroom); above that the ByteBufferWriter throws BufferOverflowException mid-send.",
  "Sandbox_Storm_ScreenshotUploadKbPerSec": "Screenshot upload rate cap (KiB/s)",
  "Sandbox_Storm_ScreenshotUploadKbPerSec_tooltip": "Wall-clock throughput cap on how fast a client streams a captured /screenshot back to the server, in KiB/s of base64 wire bytes. This is the disconnect fix: the upload must stay well under the player's uplink so RakNet's ACK/keepalive traffic keeps flowing. If the upload outruns the uplink, the reliable send backlog grows past RakNet's ~10 s connection timeout and the player is dropped with Connection Lost — and that backlog scales with screenshot resolution, which is why players on large monitors were disconnected. Default 128 KiB/s (~1 Mbit), safe on virtually any home uplink; raise it on low-latency wired networks for faster uploads. A full-resolution 4K screenshot may take a minute or more to upload at the default — this is invisible to the player and runs in the background.",
  "Sandbox_Storm_ScreenshotEncodeKbPerTick": "Screenshot base64 encode budget (KiB/tick)",
  "Sandbox_Storm_ScreenshotEncodeKbPerTick_tooltip": "Maximum source KiB base64-encoded per client tick while uploading a /screenshot. This is the client-lag fix: base64 runs on the single Lua main thread, so encoding a whole piece per frame stalled rendering for the entire upload. Encoding is also demand-driven (it pauses once enough packets are buffered ahead of the throttled sender), so with a small budget most ticks do little or no work. Default 4 KiB/tick. Lower = smoother frames but slower to fill each packet; higher = faster encode but larger per-frame hitches."
}
