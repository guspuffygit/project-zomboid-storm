{
  "Sandbox_Storm": "Storm | Performance",
  "Sandbox_Storm_ServerFps": "Server FPS (tick rate + lockFps + physics FPS)",
  "Sandbox_Storm_ServerFps_tooltip": "Unified server FPS. Drives the main-loop tick gate (1000/fps ms), PerformanceSettings.getLockFPS() on the server, and the IsoPhysicsObject.update() FPS scalar — all three together. 10 = vanilla 10 TPS. Higher = faster simulation but more CPU.",
  "Sandbox_Storm_AnimalLOSTickInterval": "Animal LOS tick interval",
  "Sandbox_Storm_AnimalLOSTickInterval_tooltip": "Per-animal stride for IsoAnimal.updateLOS(). 1 = vanilla every-tick. Larger = each animal scans LOS every Nth tick (cheaper). 0 disables LOS entirely.",
  "Sandbox_Storm_ZombieCullThreshold": "Zombie cull threshold (0 = disabled)",
  "Sandbox_Storm_ZombieCullThreshold_tooltip": "Storm-controlled cull threshold. 500 = vanilla cap (default). Larger = allow more live zombies before culling. 0 disables culling entirely (no zombies ever queued for deletion).",
  "Sandbox_Storm_ServerLosThreads": "ServerLOS worker threads",
  "Sandbox_Storm_ServerLosThreads_tooltip": "Number of concurrent player LOS scans per tick. 1 = vanilla single-threaded baseline. Up to 16. The helper pool always pre-allocates 15 threads regardless; this knob only controls how many receive work each tick.",
  "Sandbox_Storm_NetDataCapMs": "Net-data drain cap (ms, 0 = disabled)",
  "Sandbox_Storm_NetDataCapMs_tooltip": "Per-outer-loop-spin wall-clock cap on time spent inside GameServer.mainLoopDealWithNetData (HIGH-priority + player-update + vehicle inbound drain combined). Default 90. When a spin exceeds the cap, subsequent packets in that spin are short-circuited (RakNet retransmits any reliable ones); the next spin starts fresh. Protects world-tick scheduling and the RakNet outbound send buffer during reconnect storms. 0 = disabled (vanilla behaviour, no cap). Counter: pz_netdata_deferred_total.",
  "Sandbox_Storm_PeerSendBufferKickMb": "Auto-kick peer when HIGH send buffer exceeds (MB, 0 = disabled)",
  "Sandbox_Storm_PeerSendBufferKickMb_tooltip": "If a peer's outbound HIGH-priority RakNet send buffer stays above this many megabytes for 5 consecutive seconds, Storm force-disconnects the connection with reason storm-send-buffer-overflow. Protects the server from OOM when a peer on a saturated or lossy uplink accumulates the server's broadcast firehose in their per-peer RakNet queue (PZ has no backpressure anywhere in the HIGH-priority send paths, so a stalled peer otherwise grows unbounded until OOM). Suggested operational value: 35-50 MB. 0 = disabled (default, no auto-kick). Per-peer gauge: storm_peer_send_buffer_bytes{priority=\"high\"}. Counter: storm_peer_kicked_send_buffer_total."
}
